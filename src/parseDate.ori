// Parse a YYYY-MM-DD date from the start of the text.

(text) => text
  → @regexParseFn("^(?<date>\\d\\d\\d\\d-\\d\\d-\\d\\d)")

  // Dates on their own will end up in GMT, so we shift the date to the desired
  // time zone. This sample content uses U.S. Eastern Time; replace with your own
  // time zone.
  → (match) => new:@js/Date(`${ match/date } 12:00:00 EDT`)
